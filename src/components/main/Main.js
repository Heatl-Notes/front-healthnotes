import React from 'react';

import Rotas from '../../rotas';

import './Main.css';

const Main = () => {
    return (
        <main>
            <Rotas />
        </main>
    );
};

export default Main;